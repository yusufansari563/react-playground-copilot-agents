Create or update Storybook stories for changed UI components.

Requirements:

- Include practical variants (default, edge case, loading/error where relevant).
- Use clear controls for key props.
- Keep stories small and focused.

Return:

1. Stories added/updated
2. Covered component states
3. Missing scenarios (if any)
